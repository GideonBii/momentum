{
  "name": "momentum-functions",
  "description": "Firebase Cloud Functions for Momentum shared-goal reminders",
  "version": "1.0.0",
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "scripts": {
    "build": "echo \"No build step needed\"",
    "start": "firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/tasks": "^3.4.0",
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^4.6.0"
  }
}
